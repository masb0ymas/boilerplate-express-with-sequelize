// eslint-disable-next-line import/named
import { wrapperRequest } from './ExpressHelpers'
import {
  getUniqueCode,
  getUniqueCodev2,
  getToken,
  validationRequest,
} from './Common'

export {
  wrapperRequest,
  getUniqueCode,
  getUniqueCodev2,
  getToken,
  validationRequest,
}
